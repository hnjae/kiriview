# Design Review Progress

This file is an agent-facing status index for `DESIGN_REVIEW_CORRECT_END_STATE.md`. Use it to avoid reopening completed findings and to pick the next design-review task.

## Completed

- P1 HEIF/BMFF brand policy: centralized HEIF still/sequence brand kind and family membership in Rust policy and updated HEIF/input classification tests. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: a841f33a.
- P1 typed video source-load failures: preserved source URL, kind, diagnostic detail, severity, and retryability through video document state while keeping public error text unchanged; added video state/runtime regression tests and trimmed the remaining design-review backlog to backend playback errors. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 5cd4b34b.
- P1 typed video backend failures: preserved backend playback source URL, kind, diagnostic detail, severity, and retryability through video document state while keeping public backend error text unchanged; added video state/runtime regression tests and updated architecture/design-review docs. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 2f9a4ced.
- P1 thumbnail source identity: replaced the active-navigation thumbnail runtime's local source-key type with canonical `ThumbnailSourceKey` and explicit generation freshness checks; added source-key, runtime, and architecture-boundary coverage and removed the completed backlog item. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: bdb3b78e.
- P1 predecode runtime fact injection: injected timer scheduler and thread-count providers through image and media predecode dependency paths, replaced coordinator/controller wall-clock waits with manual timer tests, and updated architecture/design-review docs. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: a22b26ac.
- P1 session Open With runtime extraction: moved displayed-media Open With provider invocation, cancellation, and stale-completion rejection from DocumentSessionRuntime into a focused session runtime while preserving target selection, failure, and stale-completion behavior; added focused controller tests and updated architecture/design-review docs. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 1df2b606.
- P1 session video-output attachment port: moved accepted video-output attach/detach effect application into `DocumentSessionVideoOutputRuntime` behind a narrow attachment port, leaving `DocumentSessionRuntime` responsible for public-projection eligibility gating; added focused video-output runtime coverage. Verified with `env CI=1 NO_COLOR=1 KIRIVIEW_TEST_JOBS=1 devenv shell -- devenv tasks run --mode single ci`. Commit: 5fc78856.
- P1 live directory watch provider seam: routed live image page candidate directory watching through an injected provider and moved KCoreDirLister into the production adapter; updated async architecture plus fake-provider directory entry/store and architecture-boundary tests. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 44f7917c.
- P1 active-navigation thumbnail demand ownership: wired production thumbnail demand coalescing through ActiveNavigationThumbnailDemandTracker and covered interleaved row demand behavior in focused C++ tests; removed the completed parallel-tracker backlog item from the design review docs. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 714089eb.
- P1 typed primary image load failures: preserved source URL, session id, kind, diagnostic detail, severity, and retryability for primary image data-load, decode, opened-collection, and presentation failures while keeping QML error text unchanged; added state, loader, runtime, and architecture coverage and narrowed the remaining design-review backlog to lower-level decoder/tile diagnostics. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: a0fc102a.
- P2 shortcut scope validity: centralized C++ `ImageShortcutScope` validity in the application type boundary and guarded image, active-media, and video shortcut availability against unknown scopes; added focused shortcut policy coverage and removed the completed backlog item. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: ab0ba6c0.
- P2 zoom preset descriptors: centralized preset action names, action text, menu text, and percent payloads in one application descriptor table and aligned metadata/dispatch tests plus generated catalog references. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 28012c59.
- P2 media entry source boundary: resolved image-document source-load requests to opened-collection scopes before calling MediaEntrySourceStore and kept archive store tests free of document request types; added architecture-boundary and source-load scope tests. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 11c78ee2.
- P2 navigation source identity: extracted fact-based navigation-source URL derivation so document-portal host paths and KIOFuse runtime dirs are testable without xattrs or process environment mutation; updated architecture/state-ownership docs and image URL/source-key coverage. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: ed571730.
- P2 typed KIO operation failures: preserved operation kind, target URL, raw KJob/KIO code, cancellation status, user text, diagnostic detail, and retryability for deletion and Open With provider failures while keeping public UI text unchanged; added system, deletion runtime, Open With runtime, image deletion, and session coverage plus architecture docs. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 2bbb458c.
- P2 QImageReader tile diagnostics: preserved ordered reader-clip, source-clip, scaled-level, and full-image fallback tile decode diagnostics while keeping the existing string error adapter stable; added focused QImageReader tile source coverage and updated architecture/design-review docs. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: ce2e4acb.
- P2 image format capability catalog: centralized RAW extension ownership and decoder-family capability metadata in Rust image format policy while keeping C++ decode routing as execution; added Rust catalog/classification alignment tests, C++ Qt raster route coverage, and architecture docs. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 90d9ba6d.
- P2 image load source resolution: passed resolved opened-collection source facts into image-load planning and moved production local-directory probing to the session tracker; added architecture and planner coverage. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: b5df5350.
- P1 viewport facade characterization: covered centered anchored zoom, pan/final scan commands, and next-displayed-image final scan start at the KiriImageDocument boundary; added focused facade characterization tests. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 3ec4b670.
- P2 route phase-order characterization: characterized mutation, publication, and follow-up operation ordering across empty, direct image, direct video, image-document, and deletion-fallback route plans; added focused route-plan coverage. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: d146e432.
- P2 image/video failure message characterization: pinned current image runtime failure messages for unsupported archive video, animation presentation failure, and empty opened collection while relying on existing video resolver/backend message coverage; added focused runtime assertions. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 54994bf7.
- P2 system memory budget injection: routed image-document and direct-media predecode cache budget resolution through injected system-memory snapshots and configured shared display/thumbnail provider stores from resolved dependency-layer budgets; added owner-level dependency and session coverage. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 03c37aca.
- P2 grouped command router ports: split the application command router target callbacks into owner-grouped shell, session, image-document, image-presentation, panel, window, help, and video port structs while preserving existing dispatch behavior; added an architecture-boundary guard and updated router tests. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 3f490bca.
- P2 fixed shortcut dispatch policy: extracted fixed viewer shortcut mapping for video seek, horizontal arrows, single-page arrows, and vertical pan into a pure application shortcut policy outcome consumed by the Qt runtime adapter; added policy coverage and kept the existing shortcut integration path green. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 879849a7.
- P2 image open status invariant: added application-plan final-state validation for image open status/loading/error tuples so contradictory `Ready + loading`, `Ready + error`, and `Error + empty error` plans are rejected before state mutation or runtime effects; added applier coverage and kept workflow coverage green. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 3cabc35f.
- P2 providerless image display source: made `ImagePageSurfaceController::setImage()` publish an explicit display-error source slot with image dimensions instead of leaving image state paired with a missing provider source; added controller coverage and kept display-source projection coverage green. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 7e23e24f.
- P1 session video-output claim runtime: moved video-output surface token issuance, owner freshness, detach authorization, and clear-on-mode-exit state from `DocumentSessionRuntime` into `DocumentSessionVideoOutputRuntime`; added focused runtime coverage and preserved the existing `KiriVideoDocument` session integration path. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: ae9d8b47.
- P3 media open dialog labels: moved mixed-media open-dialog filter text from `navigation/mediaformatregistry` into a facade helper, kept navigation on capability predicates/extension lists, and updated generated i18n references through project tooling. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: e0d4da0d.
- P2 stored image data loader ownership: moved default stored image data loading from `async/imageiojobs` into `decoding/imagedataloading`, kept scheduler injection through decode dependencies, and added an architecture-boundary guard that keeps decode request and `KIO::storedGet` ownership out of `src/async/imageiojobs.*`. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 705cd65c.
- P2 directory candidate loader ownership: moved directory image-page and container candidate loading from `async/imageiojobs` into `navigation/imagedocumentpagecandidateloading`, kept provider/store behavior unchanged, and added an architecture-boundary guard that keeps directory-listing ownership out of `src/async/imageiojobs.*`. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: a0178edd.
- P2 opened-collection candidate loader ownership: moved opened-collection candidate loading from `async/imageiojobs` into `archive/mediaentrysourcecandidateloading`, removed `src/async/imageiojobs.*`, and kept provider scheduler binding through the existing dependency path. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 784bd884.
- P2 collection source failure diagnostics: replaced string-only `MediaEntrySourceError` payloads with typed backend, operation, collection URL, entry path, user text, and diagnostic detail fields; directory, KArchive, libarchive, runner, and async candidate forwarding now preserve the typed payload through common result variants. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 78f8da28.
- P2 thumbnail failure diagnostics: added structured active-navigation thumbnail failure diagnostics for cache lookup, generation, generation-provider, and image-store failures, preserving job id, source key, work kind, demand bucket, failure kind, and error detail without changing the QML-facing thumbnail model. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 3bfc37c4.
- P2 static decode failure diagnostics: extended `DecodedImageFailure` with operation, diagnostic detail, severity, and retryability defaults, and tagged static image first-display/blocking-preview failures before document-level wrapping. The broader lower-level decoder diagnostics finding remains open for route/backend and animation/adapter details. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 3180c9e6.
- P2 decoder route failure diagnostics: stamped selected decode handler route onto `DecodedImageFailure` at the router execution boundary, so string-only handler failures still preserve whether SVG, APNG, HEIF, RAW, or Qt raster was selected. The broader finding remains open for backend/raw detail and animation/adapter-specific operations. Verified with devenv shell -- devenv tasks run --mode single ci. Commit: 43064ec2.
- P2 HEIF failure diagnostics: preserved HEIF route, sequence open/frame operation, and backend diagnostic detail for HEIF still and sequence failures while keeping public error text unchanged; added focused Kiri image decoder coverage. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: db520dda.
- P2 SVG/APNG failure diagnostics: preserved SVG and APNG route, operation, and adapter diagnostic detail while keeping public error text unchanged; added focused image decode pipeline coverage. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 76c6f69a.
- P2 tile decode failure metadata: preserved severity and retryability on QImageReader tile decode attempt diagnostics while keeping the existing string compatibility path unchanged; added focused tile source coverage. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 293664f0.
- P2 ready image open source invariant: extended image-open final-state validation so `Ready` transitions require a non-empty source URL and displayed image location before state mutation or runtime effects; added focused transition-applier coverage. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: fe31f4f4.
- P2 ready image open source-kind invariant: extended image-open final-state validation so `Ready` transitions reject video source kind unless the unsupported opened-collection video state is set; added focused transition-applier coverage. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 550d9c63.
- P2 null image open navigation invariant: extended image-open final-state validation so `Null` transitions reject active container-navigation URLs before state mutation or runtime effects; added focused transition-applier coverage. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 2cedf464.
- P2 ready image open navigation-source invariant: extended image-open final-state validation so `Ready` transitions reject container-navigation URLs that do not match the final displayed location's derived container navigation source; this completes the open image-open transition invariant finding through the existing validating final-state boundary. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commits: 5ebc1226, c9ccb732.
- P3 directory listing invalid-url diagnostics: rejected empty directory-listing URLs before `KCoreDirLister`, emitted a KiriView warning diagnostic for invalid empty URLs and synchronous `openUrl` refusal, and kept the public error callback text unchanged; the broader async no-op observability finding remains open for decode dependency, stale callback, and QML catch paths. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: a5e0ffbc.
- P3 toolbar menu-anchor mapping diagnostics: kept application-menu anchor usability fallback behavior unchanged while emitting a QML warning when `mapToItem()` throws; the broader async no-op observability finding remains open for decode dependency and stale callback paths. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: fa929c41.
- P3 decode invalid-request no-op diagnostics: kept empty decode requests as non-delivering no-ops while emitting KiriView warnings for empty requests and the defensive missing-dependency guard; the broader async no-op observability finding remains open for stale callback and remaining backend-listing failure classification. Focused verification: `devenv shell -- env CARGO_TARGET_DIR=target cargo build --locked --lib --all-features && devenv shell -- cmake -S tests/cpp -B target/devenv/cpp-tests -DCMAKE_BUILD_TYPE=Debug -DKIRIVIEW_CARGO_TARGET_DIR="$PWD/target/debug" && devenv shell -- cmake --build target/devenv/cpp-tests --target test_imagedecodejob --parallel "$(nproc)" && devenv shell -- env LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8 ctest --test-dir target/devenv/cpp-tests -R '^test_imagedecodejob$' --output-on-failure`. Commit: 750db372.
- P3 async no-op diagnostics resolution: emitted warning diagnostics for asynchronous directory-listing backend failures while preserving public error callback text, after earlier slices covered invalid directory URLs, synchronous listing open refusal, toolbar anchor mapping fallback, empty decode requests, and the defensive decode missing-dependency guard. Expected stale/cancel suppressions remain quiet, so the async no-op finding was removed from the open list. Focused verification: `devenv shell -- env CARGO_TARGET_DIR=target cargo build --locked --lib --all-features && devenv shell -- cmake -S tests/cpp -B target/devenv/cpp-tests -DCMAKE_BUILD_TYPE=Debug -DKIRIVIEW_CARGO_TARGET_DIR="$PWD/target/debug" && devenv shell -- cmake --build target/devenv/cpp-tests --target test_directorylistingjob --parallel "$(nproc)" && devenv shell -- env LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8 ctest --test-dir target/devenv/cpp-tests -R '^test_directorylistingjob$' --output-on-failure`. Commit: f0652aea.
- P2 provider-ready display-source invariant: added a presentation-runtime projection guard so malformed provider-ready page slots with an empty provider URL project as display errors with provider load/cache flags disabled; the broader page-slot variant refactor remains open. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 33eef7db.
- P1 direct media deletion lifecycle: moved direct-media deletion candidate loading, cancellation, and stale candidate completion rejection into `DocumentSessionMediaDeletionRuntime`, keeping `DocumentSessionRuntime` responsible for command gating, progress projection, and completion-route application; added focused deletion-runtime coverage and preserved session deletion integration. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 753d3008.
- P1 direct media predecode runtime: moved direct-media predecode schedule/cache eligibility, coordinator lifecycle, clear/cancel behavior, and cached-image lookup into `DocumentSessionMediaPredecodeRuntime`, keeping `DocumentSessionRuntime` responsible for supplying committed session and leaf facts; added focused runtime coverage and preserved session predecode integration. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: c7aa7b81.
- P1 session leaf snapshots: moved session-facing image/video leaf reads behind cohesive `DocumentSessionImageDocumentSnapshot` and `DocumentSessionVideoDocumentSnapshot` callbacks, keeping leaf command/effect ports narrow while individual notification hooks remain as residual work; added focused session-runtime coverage and preserved `KiriDocumentSession` integration. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: f294a7aa.
- P1 session thumbnail subowner: moved active-navigation thumbnail runtime construction, session-specific opened-collection source adaptation, and QML demand delegation behind `DocumentSessionThumbnailRuntime`; added focused subowner coverage and preserved lower-level thumbnail runtime plus session facade integration. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 5d91877c.
- P1 image runtime workflow owner: moved `ImageDocumentRuntimePlan` dispatch ownership from `ImageDocumentRuntimeControllers` into `ImageDocumentRuntimeWorkflow`, keeping the operation table fakeable and leaving controller construction as workflow-port wiring; added focused workflow coverage and preserved runtime-plan executor coverage. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: d5cf33a9.
- P1 direct media navigation workflow: moved direct-media navigation refresh/open completion decisions into `DocumentSessionDirectMediaNavigationWorkflow`, leaving async candidate loading and state/effect application in session runtime; added focused workflow coverage and preserved direct-navigation runtime, session leaf snapshot, and media-predecode coverage. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 105e9840.
- P1 session leaf snapshot-change events: replaced wide image/video leaf notification connector bags with snapshot-family change connectors while keeping leaf command/effect ports narrow; added focused session runtime coverage and updated architecture/design-review docs. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 4ad74f4e.
- P1 session active-navigation dispatch runtime: moved active-navigation dispatch reveal context and direct-media/image-page operation execution into a focused session runtime while keeping `DocumentSessionRuntime` as the source/snapshot adapter; added focused runtime coverage and preserved `KiriDocumentSession` integration. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 73a93bcb.
- P1 session route execution runtime: moved route-operation dispatch and route-local execution facts into `DocumentSessionRouteRuntime`, keeping `DocumentSessionRuntime` as the typed port adapter for state, leaf routing, projection, direct-navigation refresh, and predecode effects; added focused route-runtime coverage and preserved session facade integration. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: b7f7b9a8.
- P1 session split leaf ports: separated session-facing image/video snapshot ports from leaf command/effect ports, updated document-session runtime and thumbnail source adaptation to consume the narrower port families, and added an architecture-boundary guard. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: bd51eff3.
- P1 viewport scan-start runtime ownership: moved pending displayed-image final-scan start state and displayed-image initial-position command handling into `ImageDocumentRuntime`, leaving `KiriImageDocument` as a forwarding facade for that workflow; added runtime coverage and preserved facade behavior. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: ca44ddef.
- P1 viewport pan/scan runtime ownership: moved viewport pan, initial/final scan, and forward/back scan content-position command planning into `ImageDocumentRuntime`, leaving `KiriImageDocument` as a QML argument adapter for that workflow; added runtime coverage and preserved facade behavior. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 1c754672.
- P1 viewport anchored geometry runtime ownership: moved anchored manual zoom, zoom-by-step anchor resolution, fit/actual-size toggle geometry, point-inside-image checks, and nearest-image-point queries into `ImageDocumentRuntime`; removed `ImageViewportInteraction` ownership from `KiriImageDocument`, added runtime coverage, and preserved facade behavior. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 41a0726e.
- P1 viewport projection-driven command application: removed explicit post-dispatch viewport projection application from `ImageViewport` command handlers, leaving application driven by viewport-frame/projection revision changes while preserving wheel, double-click, pan, and session integration behavior; added architecture-boundary coverage. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 6c5c3f41.
- P2 route execution phases: separated route mutation traversal from publication and follow-up effect execution inside `DocumentSessionRouteRuntime`, so public projection publication is collected and run before direct-navigation refresh and predecode clearing regardless of linear plan order; added focused phase-order coverage and preserved session integration. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: f0f6b2a9.
- P2 typed route follow-up effects: split `DocumentSessionRoutePlan` into ordered mutations, a public-projection publication request, and typed follow-up effects for direct-media navigation refresh and predecode clearing; updated route runtime and deletion/session integration coverage, completing the route-plan phase-boundary finding. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: c9d72713.
- P1 viewport command bridge ownership: moved physical Flickable projection application, command revision tracking, and begin/complete/acknowledge sequencing from `ImageViewport.qml` into `KiriImageViewportCommandBridge`; updated architecture intent and boundary/viewport QML harness coverage. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 46a0f4dc.
- P1 session video command runtime: moved video document source assignment, stop, source clearing on session mode exit, and video-output attachment-port adaptation into `DocumentSessionVideoDocumentCommandRuntime`, leaving `DocumentSessionRuntime` responsible for route/projection gating and video-output claim ownership. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 54547207.
- P1 session image command runtime: moved image document source assignment, page navigation, and image-document deletion command effects into `DocumentSessionImageDocumentCommandRuntime`, leaving `DocumentSessionRuntime` responsible for route/projection gating and deletion-progress sync; added focused runtime coverage. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 52c34eba.
- P1 session route planning runtime: moved top-level source/media route plan selection into `DocumentSessionRouteRuntime`, leaving `DocumentSessionRuntime` to supply current document kind and route-effect ports; added focused route-runtime planning coverage. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 7b4bc2ad.
- P1 session projection publication runtime: moved revisioned public snapshot publication and active-navigation thumbnail-row synchronization into `DocumentSessionProjectionRuntime`, leaving `DocumentSessionRuntime` to supply snapshot inputs and projection ports; added focused projection-runtime coverage. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 9945f869.
- P1 session deletion completion runtime: moved direct-media deletion completion application into `DocumentSessionMediaDeletionCompletionRuntime`, leaving `DocumentSessionRuntime` to supply state and route ports; added focused completion-runtime coverage. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 991390c3.
- P1 session public snapshot input builder: moved public projection input assembly and displayed-media Open With availability derivation into `DocumentSessionPublicSnapshotInputBuilder`, leaving `DocumentSessionRuntime` to supply committed session and leaf facts; added focused input-builder and architecture-boundary coverage. Verified with `devenv shell -- devenv tasks run --mode single ci`. Commit: 7dc464cb.
